enum AuthStatus {
  USER_NOT_FOUND = 0,
  PASSWORD_INCORRECT = 1,
  LOGIN_SUCCESS = 2,
}

enum AuthMessage {
  EMAIL_INVALID = 'Email is invalid',
  PASSWORD_INVALID = 'Password is invalid',
}
